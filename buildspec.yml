version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11

  build:
    commands:
      - echo Building the code...
      - mvn clean install

artifacts:
  files:
    - target/aiHackathon082023-1.0-SNAPSHOT-jar-with-dependencies.jar
  discard-paths: yes
